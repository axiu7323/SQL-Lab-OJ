# SQL 安全规则

只允许 SELECT / WITH。禁止 INSERT、UPDATE、DELETE、DROP、ALTER、TRUNCATE、CREATE、REPLACE、GRANT、REVOKE、CALL、EXEC、LOAD_FILE、INTO OUTFILE、INTO DUMPFILE、SLEEP、BENCHMARK。

禁止多语句。允许末尾单个分号。SQL 最大长度默认 10000 字符。超时默认 3000ms。最大返回 1000 行。

V1 使用字符串规范化、开头白名单、多语句检查和关键词黑名单。V2 可以引入 SQL Parser。
